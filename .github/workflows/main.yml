name: main
on: [push]

jobs:
  build:
    name: build binary
    runs-on: ubuntu-latest

    steps:
        uses: action/checkout@v3
      - name: bianyi
        shell: bash
        run: |
          ls;
          make -f Makefile;
