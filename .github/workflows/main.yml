name: main
on: [push]

jobs:
  build:
    name: build binary
    runs-on: ubuntu-latest

    steps:
      - name: bianyi
        shell: bash
        run: |
          ls;
          #          make -f Makefile;
