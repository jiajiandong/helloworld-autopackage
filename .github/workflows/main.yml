name: main
on: [push]

jobs:
  build:
    name: build binary
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v2

      - name: bianyi
        run:
          make -f Makefile;
